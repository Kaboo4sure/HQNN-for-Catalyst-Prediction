# Python virtual environments
venv/
.env/

hqnn_env/
*.pth
*.lmdb
*.tar.gz
*.whl
__pycache__/

# Data & experiment folders
data/
mlruns/
results/

# Jupyter temp & cache
*.ipynb_checkpoints/
__pycache__/
*.pyc

# OS / Editor files
.DS_Store
Thumbs.db

# Because hqnn_env/ folder sits inside the Git repo, VS Code's Git integration things all the fiarchem-core files must be trackes
#   The files are alot, so we are ignoring them.
hqnn_env/
